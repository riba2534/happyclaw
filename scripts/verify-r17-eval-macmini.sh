#!/usr/bin/env bash
# ==============================================================================
# verify-r17-eval-macmini.sh
#
# HappyClaw R17: 提示词版本任务评测、人工反馈与可下载效果报告
# 生产环境 (Mac mini) 纯客户端 HTTP API 真实执行验收脚本
#
# 完全通过 HTTP API 连接部署运行的服务，绝不直接 import 内部源码或触碰数据库。
# 严格拒绝任何模拟数据或 FakeProvider 兜底，失败必须返回非 0。
# ==============================================================================

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

echo "============================================================"
echo "  HappyClaw R17 生产环境真实端到端验收 (Mac mini)"
echo "============================================================"

# 1. 运行代码编译检查与构建完整性
echo "[1/2] 验证代码静态类型与产物构建状态..."
npm run typecheck
npm run docs:check
npm run format:check

# 2. 调用纯客户端 HTTP API 驱动器执行端到端验证
echo "[2/2] 运行 HTTP API 验收驱动器 (全协议端到端测试)..."
npx tsx scripts/verify-eval-macmini-runner.ts

echo ""
echo "============================================================"
echo "  ✓ R17 生产环境真实端到端验收成功完成！"
echo "============================================================"
