# Agent Authentication Design

This document has been merged into `docs/dev/model-switching-design.md`.

The canonical implementation spec is:

- `docs/dev/model-switching-design.md`

Do not treat this file as a separate normative design source. It remains only as
a redirect for older links.

Merged topics include:

- Claude and Codex provider families;
- official Codex OAuth and API-key auth;
- provider pools and model availability;
- auth profile generation and credential rotation;
- provider-scoped `CODEX_HOME`;
- runtime auth material and environment isolation;
- provider disable/delete behavior;
- permissions and first-version IM limitations.
